#include "ui_sys/label_decorator.h"

// template <typename T>
// DirtyLabelProvider<T>::DirtyLabelProvider(const LabelProvider<T>* provider_)
//     : provider_(provider_) {}

// template <typename T>
// auto DirtyLabelProvider<T>::LableOf(const T& obj) const -> std::string {
//   return "*" + provider_->LableOf(obj);
// }